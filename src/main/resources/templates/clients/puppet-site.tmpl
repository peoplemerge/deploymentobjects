# Warning: do not edit this file.  It is automatically generated from git://server/puppet-site.tmpl
import "roles.pp"

node 'puppetmaster1.peoplemerge.com' {
	include standard
}
#foreach( $environment in $allenvironments )
#foreach( $host in $environment.getHosts() )
node '$host.getHostname().$host.getDomainname()' {
#foreach( $role in $host.getRoles() )
	include $role.getName()
#end
}
#end
#end
