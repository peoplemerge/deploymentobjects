# Warning: do not edit this file.  It is automatically generated from git://server/puppet-hosts.tmpl
class hosts {
        host {'ino.peoplemerge.com':
                ensure => present,
                host_aliases => ['ino'],
                ip => '192.168.10.105'
        }
        host {'puppetmaster1.peoplemerge.com':
                ensure => present,
                host_aliases => ['puppetmaster1'],
                ip => '192.168.10.137'
        }
#foreach( $environment in $allenvironments )
#foreach( $host in $environment.getHosts() )
        host {'$host.getHostname().$host.getDomainname()':
                ensure => present,
                host_aliases => ['$host.getHostname()'],
                ip => '$host.getIp()'
        }
#end
#end
}
